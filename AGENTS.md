# AGENTS.md

- Use `just pokemon <pokemon name>` to print the matching entry from `pokemon_sizes/pokemon.json`.
- Name matching is case-insensitive.
- Multi-word names work, for example: `just pokemon mr. mime`.
- Quote names with shell-special characters, for example: `just pokemon "Nidoran (F)"`.
