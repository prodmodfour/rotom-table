# Track 2 validation matrix

This matrix maps the later Track 2 implementation areas to the validation expected before each area is considered ready. It is a planning and review document: it does not claim those areas are implemented yet.

Track 2 remains locked to the GM-hosted session architecture described in the [Track 2 roadmap](track-2-roadmap.md), [glossary](track-2-glossary.md), and ADRs. The matrix should be updated only to clarify validation expectations, not to replace the architecture with a SaaS, generic shared-document editor, cloud-database, or Quick Tunnel-first model.

## Baseline for every Track 2 ticket

Every implementation ticket should include the narrowest useful validation for the change it introduces:

- Run the standard checks: `npm run typecheck`, `npm test`, and `npm run build` through the controller quality gate.
- Add focused Vitest coverage for pure logic, route/use-case behaviour, message schema handling, command validation, permissions, conflicts, reconnect, or persistence when those behaviours change.
- Add manual smoke notes or runbook steps when the behaviour depends on multiple browsers, LAN devices, named tunnels, or browser WebSocket behaviour that unit tests cannot fully cover.
- Update target documentation when setup, architecture, protocol, operations, security, recovery, limitations, or user-visible session behaviour changes.
- Preserve local mode, legacy non-session paths, private data boundaries, and Track 1 render quality unless a ticket explicitly and safely changes them.
- Keep examples free of real secrets, private maps, generated sheets, session snapshots, event logs, tunnel credentials, or real `.env` files.

## Implementation-area matrix

| Later implementation area | Expected automated tests | Expected smoke checks | Expected docs | Security and safety review |
| --- | --- | --- | --- | --- |
| Session contracts and validation skeleton (tickets 010-019) | Type and helper tests for session IDs, player/client IDs, join codes, GM keys, display-name sanitization, roles, controllable resources, revisions, `opId`, command envelopes, result shapes, WebSocket message unions, validators, serialization, malformed envelopes, duplicate IDs, and permission edge cases. | Compile/import shared contracts from both client-facing and server-facing code paths; exercise accepted and rejected command examples against validators. | Protocol overview, message-flow examples, accepted/rejected command examples, glossary links, and compatibility notes for local mode. | Confirm no full-account assumptions, no public-auth claims, safe display-name handling, no leaking GM keys/join codes in examples, and fail-closed invalid schema handling. |
| Session state and local persistence (tickets 020-029) | Unit tests for in-memory store lifecycle, authoritative map/session state, revision increments, event metadata, duplicate `opId` idempotency, atomic snapshot writes, snapshot validation/recovery, optional JSON-lines event log writes, cleanup, and end-session behaviour. | Create a session, mutate state, write a snapshot, restart/recover from the latest valid snapshot, and verify stale or duplicate operations do not advance revisions unexpectedly. | Storage layout, ignored/private file guidance, backup/recovery steps, event-log limitations, cleanup policy, and local-first persistence boundaries. | Confirm snapshots/logs stay local and ignored where appropriate, no hosted database dependency is introduced, partial writes fail safely, and cleanup never silently deletes recovery data. |
| Identity, join, lobby, and assignments (tickets 030-039) | Route/use-case tests for runtime-flag enforcement, GM session start, player join with code and sanitized display name, duplicate/invalid joins, client identity continuity, management/player state endpoints, assignments, and permission decisions. | Two local browser profiles: GM starts a guarded session, player joins with a display name, GM assigns resources, player sees only allowed session state, and disabled flag hides or rejects session surfaces. | GM/player join flow, lobby management, LAN-oriented manual QA checklist, safety banner expectations, and session-local identity explanation. | Confirm `ROTOM_ENABLE_SESSION_HOST=1` or the documented equivalent is required, trust-based local role picker is not public auth, join codes/GM keys are session-local, and players cannot assign themselves resources. |
| WebSocket transport and reconnect (tickets 040-049) | Tests for gated socket route or adapter, client composable status/queue cleanup, hello/auth handshake, heartbeat ping/pong, stale connection detection, session-scoped fanout, reconnect with `lastSeenRevision`, snapshot fallback, malformed messages, and session isolation. | Local two-client WebSocket session: connect GM and player, observe presence, send heartbeat, disconnect/reconnect one client, verify replay or snapshot fallback, and confirm no cross-session broadcast leakage. | WebSocket protocol examples, heartbeat intervals and timeout expectations, reconnect flow, Cloudflare named-tunnel considerations, and the boundary with legacy SSE outside session mode. | Validate every inbound message before dispatch, reject unauthorized sockets safely, keep hosting flag fail-closed, avoid leaking presence/events across sessions, and document Cloudflare idle-timeout risks. |
| Token command flows (tickets 050-059) | Tests for `moveToken`, `turnToken`, spawn/delete, and send-out command schemas; token position/facing/range validation; assigned-token permissions; GM-only commands; revision increments; accepted patch shape; stale same-token rejection; duplicate command idempotency; optimistic ack/reject reconciliation. | Two-client token scenario: GM assigns a token, player moves/turns it, all clients receive small authoritative patches, stale same-token moves reject with current state, and local non-session token editing still works. | Token command reference, permission/conflict rules, optimistic UI behaviour, two-client smoke checklist, and known limitations. | Ensure session mode does not autosave whole maps as the main concurrency mechanism, players control only assigned/visible tokens, GM-only spawn/delete is enforced, and current token state in rejections is player-safe. |
| Table action command flows (tickets 060-069) | Tests for HP, combat stages, conditions, initiative, move/maneuver/ability/order boundaries, hazards, field effects, and terrain commands; command-specific validation; permission boundaries; revision/conflict handling; small broadcasts; and preservation of existing automation rules. | Two-client table-action scenario: apply HP/condition/initiative changes, verify both clients update, reject unauthorized player actions, and confirm map renderer invalidation remains correct for terrain/effects. | Supported table-command docs, permission and conflict matrix, limitations, and migration notes for existing move automation. | Confirm GM-only hazards/effects/terrain where required, recheck player permissions against current assignments, avoid exposing hidden sheets/state, and preserve Track 1 visual quality and map functionality. |
| Client session integration (tickets 070-079) | Tests for local/session state split, session map composable, command dispatch, optimistic ack/reject handling, MapScenePanel routing, rejection UI, presence UI, reconnect states, navigation entry points, cleanup, and local-mode no-regression paths. | Open local mode and session mode side by side; verify local autosave remains local-only, session actions use commands, rejection and reconnect states are visible, and basic token propagation works across tabs. | Local-mode vs session-mode coexistence, disconnect/reconnect recovery guidance, conflict/retry guidance, navigation/lobby entry points, and multi-tab smoke script or checklist. | Confirm session clients do not rely on whole-map autosave for live concurrency, UI does not imply hardened public auth, stale optimistic state is reconciled from server authority, and Track 1 render quality is unchanged. |
| Hosting hardening and operations (tickets 080-089) | Tests for runtime flag parsing, disabled-by-default behaviour, safe startup warnings, host command/script helpers, unsafe exposure checks, session-host disabled mode, and any backup/recovery helpers. | LAN smoke with GM plus players on separate browsers/devices; named Cloudflare Tunnel smoke with stable hostname; stop/restart tunnel and app; verify reconnect, token move, initiative, and conflict rejection. | LAN runbook, named Cloudflare Tunnel runbook, Quick Tunnel caveat, session-host command/script, public exposure warnings, backup/recovery docs, security review, deployment smoke checklist, and dependency/runtime review. | Ensure Quick Tunnel is documented as dev smoke-test only, named tunnel is the supported remote path, public exposure warnings are clear, no tunnel tokens or real `.env` files are committed, and no cloud database or SaaS requirement is added. |
| Final audit and completion (tickets 090-099) | Integrated regression tests or audit checks covering move/turn/HP/conditions/initiative/reconnect/permissions/stale conflicts, plus full quality gate results after all implementation chunks land. | Recorded multi-client command audit, LAN smoke pass, named-tunnel docs review, local-mode regression audit, security audit, persistence/recovery audit, and concurrency/latency observations. | Final implementation review linking chunk PRs, tests, runbooks, audits, known limitations, benchmark notes, and any cleaned-up temporary Track 2 references. | Confirm remaining limitations are explicit, no private/generated data or secrets are committed, local mode still works, trust boundaries are documented, and automation status is updated only after honest pass/blocker review. |

## Cross-cutting validation checklist

Use this checklist when reviewing any Track 2 implementation area:

- **Architecture lock:** The change keeps the product as a GM-hosted table session, not SaaS, public multi-tenancy, a generic collaborative document editor, or a cloud-first rewrite.
- **Runtime gate:** Session hosting remains disabled by default and fails closed unless the documented session-host flag is enabled.
- **Server authority:** Live session state changes go through validated server-authoritative commands, not live-client whole-map autosave.
- **Revisions and idempotency:** Accepted commands advance monotonic revisions exactly once; duplicate `opId` retries are idempotent; rejections do not advance revisions.
- **Permissions:** Players can affect only assigned and visible resources, while GM actions still pass validation and safety checks.
- **Reconnect:** Clients recover from server state through replay or snapshot fallback instead of trusting stale optimistic edits.
- **Persistence:** Session snapshots and optional event logs are local JSON files with atomic-write and backup/recovery expectations; no Postgres, Redis, Durable Objects, or hosted database is introduced.
- **Transport:** New session concurrency uses WebSockets for commands, acks/rejections, broadcasts, presence, heartbeat, and reconnect; legacy SSE may remain only for non-session/local paths during migration.
- **Hosting:** LAN is the primary supported path and named Cloudflare Tunnel is the supported remote path; Quick Tunnel is development smoke-test only.
- **Data hygiene:** Private campaign data, generated sheets, snapshots, event logs, secrets, tunnel tokens, private keys, and real `.env` files stay out of git.
- **No Track 1 regression:** Existing local-first map/sheet workflows and render quality remain intact unless a later ticket documents and validates a safe change.
