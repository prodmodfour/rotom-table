# API route mutation audit

This audit lists the non-GET Nitro API surfaces that can mutate server memory, maintenance JSON, or SQLite-backed runtime state. It is scoped to private trusted-table hosting and local development. It is not a public-hosting hardening review.

GET routes are omitted unless they are relevant to the notes below; current GET endpoints are treated as read-only project/campaign queries, health checks, legacy SSE, or static data lookups. The legacy session WebSocket is included because commands through it mutate session state and write session snapshots.

`/api/maps/operations/status` is a read-only POST exception: it accepts the exact stored live-play command envelope in the request body because the envelope may be too large for a query string, then reads operation history without executing, writing, or publishing the command. `/api/maps/operations/abandon` is a hosted-write operation-history mutation: it accepts the exact retained command envelope and records a terminal `abandoned` rejection tombstone without executing the command or publishing realtime events.

## Hosted-write policy summary

`ROTOM_ENABLE_HOSTED_WRITES=1` is the exact production opt-in for persistent hosted campaign writes on routes that are covered by the hosted-write policy, including SQLite map/sheet/group-inventory writes and remaining maintenance JSON writes for non-map/sheet systems. This guard still covers filesystem-backed JSON writes and SQLite-backed live-play command writes. In non-production local development, these writes remain available without the flag. When the flag is absent or any value other than exactly `1` in production, covered routes fail closed with the shared 403-style message before invoking persistence use cases or player-profile resolution.

Map write routes are covered by the same hosted-write policy. GM map library/admin routes still require GM role. `/api/maps/save` is restricted to explicit GM setup/edit whole-map saves; player-facing map/token command routes keep the existing player-visible map and selected-profile token-control checks used by local profile play after the hosted-write gate passes. Database-backed command routes write only to a database path in private operator-controlled campaign storage, with the default at `ROTOM_CAMPAIGN_ROOT/rotom-table.sqlite`; `ROTOM_DB_PATH` overrides must stay outside the app checkout and be included in backups.

Legacy live-session maintenance routes use their separate `ROTOM_ENABLE_SESSION_HOST=1` guard and session-local credentials. They are not normal profile-based play routes and are not a substitute for the hosted-write flag on covered campaign routes.

## Route table

| Route | Classification | Hosted-write policy | Notes |
| --- | --- | --- | --- |
| `/api/campaign/next-day` | GM-only admin write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Advances the campaign day by updating affected Pokémon/trainer sheets in SQLite and publishing sheet events. |
| `/api/encounters/create-folder` | GM-only admin write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Creates an encounter-table folder under the campaign encounter-table root. |
| `/api/encounters/create` | GM-only admin write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Creates an encounter-table JSON file. |
| `/api/encounters/delete-folder` | GM-only admin write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Removes an encounter-table folder. |
| `/api/encounters/delete` | GM-only admin write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Removes an encounter-table JSON file. |
| `/api/encounters/generate` | GM-only admin write | Covered for persistent output: production non-preview generation requires `ROTOM_ENABLE_HOSTED_WRITES=1`; preview uses temporary output. | Rolls an encounter table and can write generated Pokémon sheets under the campaign sheet root. |
| `/api/encounters/move-folder` | GM-only admin write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Moves an encounter-table folder. |
| `/api/encounters/move` | GM-only admin write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Moves an encounter-table JSON file between folders. |
| `/api/encounters/rename` | GM-only admin write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Renames an encounter-table JSON file. |
| `/api/encounters/save` | GM-only admin write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Rewrites an encounter-table JSON file. |
| `/api/group-inventory/save` | GM-only setup/maintenance inventory write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Saves the campaign-level group inventory document in SQLite after validating GM role, writable campaign mode, `slug`, `expectedRevision`, and a full normalized document. Stale revisions reject with reload-before-saving conflict semantics; changed saves publish a durable group inventory realtime update after commit. |
| `/api/group-inventory/transfer-to-trainer` | Player-authorized inventory transfer write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Atomically moves item quantities from the campaign-level group inventory into a trainer sheet in SQLite. GMs can target any trainer; player requests must include `profileId` for a selected player profile linked to the target trainer sheet. The route validates both document revisions, rejects stale, invalid, profileless, or unlinked transfers, and rolls back both documents if either write fails. |
| `/api/group-inventory/transfer-to-group` | Player-authorized inventory transfer write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Atomically moves item quantities from a trainer sheet into the campaign-level group inventory in SQLite. GMs can source any trainer; player requests must include `profileId` for a selected player profile linked to the source trainer sheet. The route validates both document revisions, rejects stale, invalid, profileless, or unlinked transfers, and rolls back both documents if either write fails. |
| `/api/maps/create-folder` | GM-only admin write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Creates a logical map folder in SQLite. |
| `/api/maps/create` | GM-only admin write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Creates a SQLite map document and publishes map library events. |
| `/api/maps/delete-folder` | GM-only admin write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Recursively removes a logical SQLite map folder, contained maps, mode rows, and operation history, then publishes map library events. |
| `/api/maps/delete` | GM-only admin write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Removes a SQLite map document plus related interaction-mode and operation-history rows. |
| `/api/maps/move-folder` | GM-only admin write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Moves a logical SQLite map folder, retargeting descendant folders/maps and advancing affected map revisions once. |
| `/api/maps/move` | GM-only admin write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Moves a SQLite map document to another logical folder and advances its revision once. |
| `/api/maps/rename` | GM-only admin write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Renames a SQLite map document, migrates/cleans related mode and operation-history rows, and publishes old/new map events. |
| `/api/maps/save` | GM setup/edit whole-map write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Requires GM role, explicit `interactionMode: "setup-edit"`, shared map mode **Prepare Map**, and `expectedRevision`; stale saves reject with 409 semantics. There is no player-owned whole-map merge path on this route; live gameplay uses command routes. |
| `/api/maps/interaction-mode` | GM-only shared map-mode write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Persists only the shared server-side map mode and broadcasts `map-interaction-mode-updated`; it does not copy JSON, replace map documents, or change map revisions. |
| `/api/maps/action-event` | Player-authorized transient realtime broadcast | Not a hosted filesystem write. | Publishes bounded visual-only `map-action` events to `map:<slug>` after checking the map, actor placement, player-visible boundary, and selected-profile token control. It does not write map JSON, sheet JSON, logs, metadata, campaign state, or session snapshots. |
| `/api/maps/operations/status` | Authenticated read-only operation-status query | Read-only POST exception; hosted-write opt-in is not required. | Requires GM or player role, validates the exact command envelope and current map/profile access boundary, reads SQLite operation history by `mapSlug`/`opId`, and returns `unknown` or the original accepted/rejected terminal result, including `abandoned` rejections. It never executes the command, writes campaign state, or publishes realtime events. |
| `/api/maps/operations/abandon` | Authenticated operation-history tombstone write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Requires GM or player role, validates the exact command envelope and current map/profile access boundary, serializes on the live-play map write queue, and stores a terminal `abandoned` rejection when no accepted/rejected result already exists for the same command hash. It never executes the gameplay command, changes map/sheet documents, advances map revision, or publishes realtime events. |
| `/api/maps/move-responses/pending` | Authenticated pending-window query | Read-only GET; hosted-write opt-in is not required. | Requires current map access and resolves the optional selected profile. Returns only windows owned by that profile/controller or visible to a GM, projected without ownership IDs, operation IDs, target identities, reads, rolls, or traces. Hidden maps reject before private rows are listed. |
| `/api/maps/move-responses/choose`, `/react`, `/pass` | Authenticated durable move-response command routes | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Strictly parse ID-only response wire shapes plus the selected profile authorization ID and reject mechanics, scope, patch, malformed, and oversized data. The contextual parser resolves current references, then requires actor/target/placement/profile/side ownership before exposing an option. The resume use case revalidates the full durable read set, deterministically replays prior rolls, records the response, and atomically opens the next window or commits the terminal plan/result. Exact duplicate response operation IDs return their stored result without rerunning mechanics. |
| `/api/maps/move-responses/cancel`, `/force-resolve` | GM-only reserved move-resolution termination routes | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Strict schemas and GM authorization are active, but termination/correction orchestration remains reserved for the dedicated lifecycle ticket; an otherwise valid command currently terminates with `501` and writes nothing. |
| `/api/maps/abilities/declarations` | Player-authorized native Ability declaration write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Strictly accepts a declaration command plus selected-profile authorization ID. The server loads current map/sheet authority, resolves the exact manifest runtime and effective Ability, derives controller-safe options, and persists a short-lived private offer. It accepts no target legality, trigger conclusion, roll, effect program, or patch from the client. |
| `/api/maps/abilities/resolve` | Player-authorized native Ability resolution write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Strictly accepts the server offer identity/hash and stable option IDs, then revalidates authorization, runtime, revisions, targets, resources, and complete reads. Immediate plans commit map/sheet/operation evidence and a bounded realtime reconciliation event atomically; human waits persist a private durable saga. Exact retries return stored results without rerunning mechanics. |
| `/api/maps/tokens/spawn` | GM-only map command write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Spawns a token through a canonical `spawnToken` command, validates sheet references, map bounds, placement ID uniqueness, stale revisions, and duplicate `opId` retries, persists the authoritative map revision through SQLite, and returns a `map.placements` patch. Player requests are rejected. |
| `/api/maps/tokens/delete` | GM-only map command write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Deletes a token through a canonical `deleteToken` command, clears active initiative when needed, persists the authoritative map revision through SQLite, stores the `opId` result, and returns a `map.placements` patch. Player requests are rejected. |
| `/api/maps/tokens/move` | Player-authorized profile/map command write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Moves a controllable token through a live-play command, validates the selected profile, and persists the authoritative map revision through SQLite before publishing a `live-play-command-accepted` token patch. Players must select a profile linked to that token's sheet. |
| `/api/maps/tokens/turn` | Player-authorized profile/map command write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Turns a controllable token through a live-play command, validates the selected profile, and persists the authoritative map revision through SQLite before publishing a `live-play-command-accepted` token patch. Players must select a profile linked to that token's sheet. |
| `/api/maps/tokens/modify-hp` | Player-authorized profile/map/sheet command write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Updates HP and injuries through a live-play command, validates the selected profile, resolves the backing sheet, and persists map/sheet revisions plus the `opId` result transactionally through SQLite. Players must select a profile linked to that token's sheet. |
| `/api/maps/tokens/modify-combat-stages` | Player-authorized profile/map/sheet command write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Updates combat stages through a live-play command, validates the selected profile, resolves the backing sheet, and persists map/sheet revisions plus the `opId` result transactionally through SQLite. Players must select a profile linked to that token's sheet. |
| `/api/maps/tokens/modify-conditions` | Player-authorized profile/map/sheet command write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Adds, removes, or replaces token conditions through a live-play command, validates the selected profile, resolves the backing sheet, and persists map/sheet revisions plus the `opId` result transactionally through SQLite. Players must select a profile linked to that token's sheet. |
| `/api/maps/tokens/use-ability` | Authenticated retired-route tombstone | No mutation; production still requires hosted-write mode before exposing the former write-route boundary. | Returns `410 Gone` after authentication. It parses no body, loads no map/sheet, stores no operation result, advances no revision, and publishes no event. Native clients use `/api/maps/abilities/declarations` and `/api/maps/abilities/resolve`. |
| `/api/maps/tokens/use-maneuver` | Player-authorized profile/map command write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Accepts canonical `useManeuver` command envelopes, validates selected-profile control of the acting token, writes maneuver log metadata through SQLite, stores the `opId` result, and publishes accepted patches. |
| `/api/maps/tokens/use-order` | Player-authorized profile/map command write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Accepts canonical `useOrder` command envelopes, validates selected-profile control of the acting token, writes order log and active-order effect metadata through SQLite, stores the `opId` result, and publishes accepted patches. |
| `/api/maps/use-move` | Player-authorized profile/map/sheet command write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Accepts canonical `useMove` command envelopes for EOT/Scene map usage, Daily sheet usage, and untracked move action logging through the live-play executor. The route resolves the acting token and backing sheet, persists affected map/sheet revisions plus the `opId` result transactionally through SQLite, and rejects non-envelope split usage-save requests. Players must select a profile linked to the acting token's sheet. |
| `/api/maps/tokens/resolve-move` | Player-authorized profile/map/sheet move-resolution command write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Accepts canonical `resolveMove` command envelopes, requires an exact authoritative map revision, resolves the move once on the server, validates conservative scopes against actual planned writes, transactionally persists the map, changed sheets, and `opId` result through SQLite, and broadcasts one `move.state` accepted-command patch plus full changed sheet updates. Players must select a profile linked to the acting token's sheet; targets need not be controlled. |
| `/api/maps/initiative/set` | GM-only map command write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Accepts canonical `setInitiative` command envelopes, validates token IDs, initiative values, active combatant IDs, and rounds, persists the authoritative map revision through SQLite, and returns a `map.initiative` patch with previous/current lane state. Player requests are rejected. |
| `/api/maps/initiative/next` | GM-only map command write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Advances the active initiative token through a canonical `nextInitiative` command, persists the authoritative map revision through SQLite, stores the `opId` result, and publishes a `map.initiative` accepted-command patch. Player requests are rejected. |
| `/api/maps/initiative/previous` | GM-only map command write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Moves the active initiative token backward through a canonical `previousInitiative` command, persists the authoritative map revision through SQLite, stores the `opId` result, and publishes a `map.initiative` accepted-command patch. Player requests are rejected. |
| `/api/maps/hazards/place` | GM-only map command write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Places or layers a battlefield hazard through a canonical `placeHazard` command, validates map bounds and hazard kind/layer, persists the authoritative map revision through SQLite, and returns previous/current hazard cell state. Player requests are rejected. |
| `/api/maps/hazards/remove` | GM-only map command write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Removes one hazard kind or all hazards at a cell through a canonical `removeHazard` command, validates map bounds and no-op cases, persists the authoritative map revision through SQLite, and returns previous/current hazard cell state. Player requests are rejected. |
| `/api/maps/hazards/edit` | GM-only map command write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Applies a bounded `editHazards` batch with upsert/remove operations, validates hazard kind/layer/owner, map bounds, stale revisions, and conflict scopes before committing one authoritative SQLite transaction, stores the `opId` result, and returns changed hazard cells plus the final hazard lane. Player requests are rejected. |
| `/api/maps/terrain/build` | GM-only map command write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Builds or replaces one terrain voxel through a canonical `buildTerrainVoxel` command, validates map bounds, material palette rules, token occupancy, no-op cases, and stale revisions, persists the authoritative map revision through SQLite, and returns previous/current terrain cell state plus renderer invalidation labels. Player requests are rejected. |
| `/api/maps/terrain/remove` | GM-only map command write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Removes one terrain voxel through a canonical `removeTerrainVoxel` command, validates map bounds, no-op cases, and stale revisions, persists the authoritative map revision through SQLite, and returns previous/current terrain cell state plus renderer invalidation labels. Player requests are rejected. |
| `/api/maps/field-effects/set` | GM-only map command write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Sets weather, terrain, or room effects through a canonical `setFieldEffect` command, validates category/kind/duration options, persists the authoritative map revision through SQLite, and returns previous/current field-effect lane state. Player requests are rejected. |
| `/api/maps/field-effects/remove` | GM-only map command write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Removes a field-effect kind, category, or all field effects through a canonical `removeFieldEffect` command, validates no-op cases, persists the authoritative map revision through SQLite, and returns previous/current field-effect lane state. Player requests are rejected. |
| `/api/maps/field-effects/tick` | GM-only map command write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Ticks finite weather, terrain, and room durations through a canonical `tickFieldEffectDurations` command, removes expired effects, persists the authoritative map revision through SQLite, and returns previous/current field-effect lane state. Player requests are rejected. |
| `/api/player-profiles/create` | GM-only admin write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Creates a persistent player profile JSON file under `data/player-profiles/`. |
| `/api/player-profiles/update` | GM-only admin write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Updates persistent player profile display names and linked character refs. |
| `/api/pokedex/restore-from-books` | GM-only admin write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Writes the campaign Pokédex override diff from local markdown source material; app-owned `data/reference/pokedex.json` remains unchanged. |
| `/api/pokedex/update` | GM-only admin write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Writes a campaign Pokédex override diff under `data/reference-overrides/pokedex.json`. Treat this as operator-controlled campaign reference maintenance, not app reference maintenance. |
| `/api/pokedex/profile-image` | GM-only admin write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Writes a cropped Pokémon profile image override under `assets/profile-sprites/pokemon/`; app-owned generated profile images under `public/profile-sprites/` remain unchanged. |
| `/api/sessions/assignments` | Legacy session maintenance | Separate guard: `ROTOM_ENABLE_SESSION_HOST=1` plus GM session credentials; not covered by hosted-write policy. | Updates legacy live-session assignments in memory and writes session snapshots under `data/sessions/`. |
| `/api/sessions/join` | Legacy session maintenance | Separate guard: `ROTOM_ENABLE_SESSION_HOST=1` plus join/session checks; not covered by hosted-write policy. | Creates or selects a legacy session-local player identity and writes a session snapshot. |
| `/api/sessions/manage` | Legacy session maintenance | Separate guard: `ROTOM_ENABLE_SESSION_HOST=1` plus GM session credentials; no campaign write. | Reads legacy session management state through POST to keep GM credentials out of query strings. |
| `/api/sessions/player-state` | Legacy session maintenance | Separate guard: `ROTOM_ENABLE_SESSION_HOST=1` plus player session credentials; no campaign write. | Reads one legacy player's session state through POST to keep credentials out of query strings. |
| `/api/sessions/socket` | Legacy session maintenance | Separate guard: `ROTOM_ENABLE_SESSION_HOST=1` plus session socket authentication; not covered by hosted-write policy. | WebSocket commands mutate authoritative legacy session state and can write session snapshots/event logs. |
| `/api/sessions/start` | Legacy session maintenance | Separate guard: `ROTOM_ENABLE_SESSION_HOST=1` plus GM role; not covered by hosted-write policy. | Starts a legacy GM-hosted session, creates in-memory authority, and writes an initial snapshot. |
| `/api/sheets/create-folder` | GM-only admin write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Creates a logical Pokémon/trainer sheet folder in SQLite. |
| `/api/sheets/create` | GM-only admin write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Creates a SQLite Pokémon or trainer sheet document and publishes sheet events. |
| `/api/sheets/delete-folder` | GM-only admin write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Deletes a logical SQLite sheet folder and contained sheets; map placement cleanup for deleted sheets is transactional. |
| `/api/sheets/delete` | GM-only admin write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Deletes a SQLite Pokémon or trainer sheet document, transactionally removes/retargets map references according to product semantics, and publishes sheet/map events after commit. |
| `/api/sheets/move-folder` | GM-only admin write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Moves a logical SQLite Pokémon/trainer sheet folder and advances affected sheet revisions once. |
| `/api/sheets/move` | GM-only admin write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Moves a SQLite Pokémon or trainer sheet document to another logical folder and publishes sheet events. |
| `/api/sheets/rename` | GM-only admin write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Renames a SQLite Pokémon or trainer sheet and transactionally retargets map placements/revisions before publishing sheet and map events. |
| `/api/sheets/save` | Setup/edit sheet write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Requires explicit `interactionMode: "setup-edit"` and `expectedRevision`. Saves the SQLite Pokémon/trainer sheet for editor/setup workflows; stale saves reject with 409 semantics. GM saves are unrestricted by profile; player saves outside live play still require selected-profile sheet access or public sheet access. Live map combat mutations must use command routes instead of direct whole-sheet saves. |
| `/api/shops/checkout` | Player-authorized shop live-play command write | Covered: production requires `ROTOM_ENABLE_HOSTED_WRITES=1`. | Accepts only a full `shopCheckout` live-play command envelope plus existing top-level `clientId`/player `profileId` context. It dispatches through the shop checkout use case, stores terminal `opId` results in `shop_checkout_ops`, atomically updates the authoritative shop, trainer sheet, and/or group inventory documents, appends durable realtime rows for accepted document changes plus terminal accepted/rejected checkout results, and returns accepted/rejected terminal command results with authoritative changed documents. Plain non-command checkout payloads are rejected before dispatch. |

## Remaining limitations

- The hosted-write flag does not make the trust-based GM/Player role picker public authentication. Private VPS use still requires an outer access gate.
- Hosted-write enablement does not run JSON-to-SQLite imports or schema/data migrations for the operator. Migration/import commands are explicit maintenance actions, and database/WAL files must be part of private backup and restore practice.
- Legacy session routes remain maintenance-only surfaces with their own runtime flag and credentials. They should not be used to justify public exposure of normal campaign routes.
