import{describe,expect,it}from'vitest';import manifestJson from'../../data/move-automation/manifest.json';import{FINAL_STAGE_266_272_HANDLER_REGISTRATION}from'~~/server/domain/moveAutomation/handlers/finalStage266_272';import{REGISTERED_MOVE_HANDLER_REGISTRY,validateRegisteredMoveHandlerOutput}from'~~/server/domain/moveAutomation/handlers/registry';import{registeredMoveAutomationRuntimeFor}from'~~/server/domain/moveAutomation/registry';import{FINAL_STAGE_COHORTS_266_272_MOVE_SPEC_REGISTRATIONS,MA_266_272_MOVE_NAMES,type FinalStageCohort266272MoveName}from'~~/server/domain/moveAutomation/specs/finalStageCohorts266_272';import{validateMoveSpec}from'~~/server/domain/moveAutomation/validateSpec';import{MA_266_272_SCENARIOS_BY_MOVE}from'../fixtures/moveAutomation/finalStageCohorts266_272';const context={map:{voxels:[]},intent:{schemaVersion:1,placementId:'actor',targetBranchId:'electric',selection:{}},actor:{placement:{id:'actor'},token:{abilityNames:['Blaze']}},candidatePlacements:[],selectedPlacements:[{id:'target'}],resolvedSheets:[],ruleset:{},reads:{recordPlacement:()=>undefined},queries:{weather:{active:()=>[{kind:'sunny'}]},targetStates:{resolve:()=>({typeIds:['fire']})}}};const run=(moveName:string)=>validateRegisteredMoveHandlerOutput(FINAL_STAGE_266_272_HANDLER_REGISTRATION.run({...context,intent:{...context.intent,moveName}}as never));describe('MA-266 through MA-272 final stage cohorts',()=>{it('registers exactly 55 reviewed definitions and metadata hashes',()=>{expect(FINAL_STAGE_COHORTS_266_272_MOVE_SPEC_REGISTRATIONS.map(x=>x.canonicalId)).toEqual(MA_266_272_MOVE_NAMES);expect(new Set(MA_266_272_MOVE_NAMES).size).toBe(55);for(const r of FINAL_STAGE_COHORTS_266_272_MOVE_SPEC_REGISTRATIONS){const n=r.canonicalId as FinalStageCohort266272MoveName,row=manifestJson.moves.find(x=>x.canonicalId===n)!,d=validateMoveSpec(r.spec,{capabilityIds:row.capabilityTags,rulesetVersion:row.rulesProvenance,handlerRegistry:REGISTERED_MOVE_HANDLER_REGISTRY});expect(row.runtime.definitionHash).toBe(d.definitionHash);expect(row.baseStatus).toBe('complete');expect(row.scenarioIds).toEqual(MA_266_272_SCENARIOS_BY_MOVE[n].map(x=>x.scenarioId));expect(registeredMoveAutomationRuntimeFor(n)?.definitionHash).toBe(d.definitionHash)}});it('emits strict programs for every move',()=>{for(const n of MA_266_272_MOVE_NAMES){expect(()=>run(n),n).not.toThrow();expect(run(n).operations.length,n).toBeGreaterThan(0)}});it('encodes stat source, critical, overlay, and grounding mechanics',()=>{expect(run('Foul Play').operations).toEqual(expect.arrayContaining([expect.objectContaining({kind:'damage',payload:expect.objectContaining({attackStat:expect.objectContaining({subject:{kind:'current-target'}})})})]));expect(run('Storm Throw').operations).toEqual(expect.arrayContaining([expect.objectContaining({kind:'damage',payload:expect.objectContaining({criticalHit:expect.objectContaining({trigger:{kind:'range',minimum:1}})})})]));expect(run('Magic Powder').operations).toEqual(expect.arrayContaining([expect.objectContaining({kind:'temporary-effect',payload:expect.objectContaining({definition:expect.objectContaining({kind:'creature-rule-overlay'})})})]));expect(run('Thousand Arrows').operations).toEqual(expect.arrayContaining([expect.objectContaining({kind:'damage',payload:expect.objectContaining({typeEffectiveness:expect.objectContaining({immunity:'ignore',passiveImmunity:'ignore'})})}),expect.objectContaining({id:'thousand-arrows.grounded'})]))});it('encodes stage swaps, weather growth, and status cleanup',()=>{expect(run('Guard Swap').operations.filter(x=>x.kind==='combat-stage')).toHaveLength(2);expect(run('Growth').operations.filter(x=>x.kind==='combat-stage').map((x:any)=>x.payload.value)).toEqual([2,2]);expect(run('Take Heart').operations).toEqual(expect.arrayContaining([expect.objectContaining({kind:'condition',payload:expect.objectContaining({action:'clear'})})]))})});
